[package]
name = "gates-rs"
version = "0.1.0-beta"
edition = "2024"
description = "Framework for exploration"
keywords = ["server", "http"]

[dependencies]
prime = { path = "./core", version = "0.1.2" }

[workspace]
members = ["core", "main"]
